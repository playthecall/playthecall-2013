<% content_for :head do %>
  <%= stylesheet_link_tag "pages" %>
<% end %>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'sign_in' }) do |f| %>
  <h2 class="title">Login</h2>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <% if devise_mapping.rememberable? -%>
    <br />
    <br />
    <%= f.check_box :remember_me %>
    <%= f.label :remember_me, :style => 'display:inline' %>
    <br />
  <% end -%>

  <%- if devise_mapping.recoverable? && controller_name != 'passwords' %>
    <%= link_to t("actions.forgot_password"), new_password_path(resource_name), :class => 'cancel_account' %><br />
  <% end -%>

  <%= f.submit t("actions.sign_in") %>
  <br />
<% end %>
